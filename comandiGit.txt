git add .
git status
git rm -f file.txt 
git rm --cached file.txt

git config --global user.email "<email>"
git config --global user.name "<name>"
git config --help

git commit -m "<messaggio>"
git commit -a # committa automaticamente solo i file già aggiunti e se ho eliminato file da git li elimina del tutto
git log

git checkout <id da git log>
git checkout master

git revert <id da git log>