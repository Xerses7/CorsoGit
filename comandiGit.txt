git add .
git status
git rm -f file.txt 
git rm --cached file.txt

git config --global user.email "<email>"
git config --global user.name "<name>"
git config --help

git commit -m "<messaggio>"
git commit -a
git log
git log --oneline
# nuovo commento del branch dirs

git checkout <id da git log>
git checkout master

git revert <id da git log>

git reset --hard <id del commit> # cancella tutti i commit precedenti e porta il master a quello scelto

git rm -r --cached . # da fare dopo creazione gitignore a metà progetto

# commento del branch dirs